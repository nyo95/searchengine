// TODO: Settings page is out of scope for the Material Search & Project Scheduler app.
// Keeping route for backward compatibility but not linked in navigation.

export default function SettingsPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold">Settings</h1>
      <p className="text-muted-foreground mt-2">
        This page is not part of the current app scope.
      </p>
    </div>
  );
}
